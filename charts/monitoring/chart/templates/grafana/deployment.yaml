apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: {{.Values.namespace}}
  labels: {{- include "monitoring.fullLabels" "grafana" | nindent 4}}
spec:
  replicas: {{.Values.grafana.replicas}}
  strategy:
    type: Recreate
    rollingUpdate: null
  selector:
    matchLabels:
      {{- include "monitoring.selectorLabels" "grafana" | nindent 6}}
  template:
    metadata:
      labels: {{- include "monitoring.fullLabels" "grafana" | nindent 8}}
    spec:
      securityContext:
        fsGroup: 472
        runAsUser: 472
        runAsNonRoot: true
      nodeSelector: {{- toYaml .Values.nodeSelector | nindent 8}}
      containers:
        - name: grafana
          image: {{.Values.grafana.image}}
          env:
            - name: GF_SECURITY_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: grafana-credentials
                  key: admin-user
            - name: GF_SECURITY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: grafana-credentials
                  key: admin-password
            - name: GF_PATHS_PROVISIONING
              value: /etc/grafana/provisioning
            - name: GF_SERVER_ROOT_URL
              value: "https://{{ .Values.adminAccess.hostname }}/grafana/"
            - name: GF_SERVER_SERVE_FROM_SUB_PATH
              value: "true"
            # --- Session and Auth Hardening ---
            - name: GF_AUTH_LOGIN_MAXIMUM_INACTIVE_LIFETIME_DURATION
              value: "30m"
            - name: GF_AUTH_LOGIN_MAXIMUM_LIFETIME_DURATION
              value: "12h"
            - name: GF_USERS_ALLOW_SIGN_UP
              value: "false"
            - name: GF_AUTH_ANONYMOUS_ENABLED
              value: "false"
            # --- SNS region for alerting ---
            - name: AWS_REGION
              value: "{{ .Values.grafana.alerting.awsRegion }}"
            # --- Unified Alerting ---
            - name: GF_UNIFIED_ALERTING_ENABLED
              value: "true"
          ports:
            - containerPort: 3000
              name: http
          volumeMounts:
            - name: data
              mountPath: /var/lib/grafana
            - name: datasources
              mountPath: /etc/grafana/provisioning/datasources
            - name: dashboard-provider
              mountPath: /etc/grafana/provisioning/dashboards
            - name: alerting
              mountPath: /etc/grafana/provisioning/alerting
            - name: dashboards
              mountPath: /var/lib/grafana/dashboards
          resources: {{- toYaml .Values.grafana.resources | nindent 12}}
          readinessProbe:
            httpGet:
              path: /grafana/api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /grafana/api/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 15
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: grafana-data
        - name: datasources
          configMap:
            name: grafana-datasources
        - name: dashboard-provider
          configMap:
            name: grafana-dashboard-provider
        - name: alerting
          configMap:
            name: grafana-alerting
        - name: dashboards
          projected:
            sources:
            {{- range $path, $_ := .Files.Glob "dashboards/*.json" }}
              - configMap:
                  name: grafana-dashboards-{{ trimSuffix ".json" (base $path) }}
            {{- end }}
