{{- if .Values.adminAccess.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rate-limit
  namespace: {{ .Values.namespace }}
spec:
  rateLimit:
    average: 100
    burst: 50
{{- end }}
