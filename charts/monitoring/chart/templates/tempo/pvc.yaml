apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tempo-data
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "monitoring.fullLabels" "tempo" | nindent 4 }}
spec:
  accessModes: [ReadWriteOnce]
  {{- if .Values.tempo.pvc.storageClass }}
  storageClassName: {{ .Values.tempo.pvc.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.tempo.pvc.size }}
